model{
  # toy model for testing negative binomial minimum count
  for(i in 1:nyear){
    N[i] ~ dunif(100, 1000)
    r[i] ~ dunif(0,5000)
  }
  for(i in 1:nyear){
    mu[i] <- r[i]/(r[i] + N[i])
    mindat[i] ~ dnegbin(mu[i], r[i])
  }
}